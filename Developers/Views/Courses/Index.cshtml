@* @model IEnumerable<Developers.Models.Course> *@
@{
    ViewData["Title"] = "Cursos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center">
    <div class="col-12 col-md-8">
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between">
                    <h2>Cursos</h2>
                    <div>
                        <a class="btn btn-primary" asp-controller="Courses" asp-action="Create">
                            <i class="bi bi-plus-square"></i> Nuevo
                        </a>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-sm table-striped" id="table_courses">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nombre</th>
                                <th>Duración</th>
                                <th>Estado</th>
                                <th width="120px">Opciones</th>
                            </tr>
                        </thead>
                        @* <tbody>
                        </tbody> *@
                    </table>
                    @* @if (Model.Count() > 0)
                    {
                        <table class="table table-sm table-striped" id="table_courses">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Nombre</th>
                                    <th>Duración</th>
                                    <th>Estado</th>
                                    <th width="120px">Opciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td> @item.CourseId </td>
                                        <td> @item.Name </td>
                                        <td> @item.Hours</td>
                                        <td>
                                            @if (item.Status)
                                            {
                                                <span class="text-success">ACTIVO</span>
                                            }
                                            else
                                            {
                                                <span class="text-danger">INACTIVO</span>
                                            }
                                        </td>
                                        <td>
                                            <a class="btn btn-sm btn-info">
                                                <i class="bi bi-list-check"></i>
                                            </a>
                                            <a class="btn btn-sm btn-warning" asp-controller="Courses" asp-action="Edit" asp-route-id="@item.CourseId">
                                                <i class="bi bi-pencil-square"></i>
                                            </a>
                                            <a class="btn btn-sm btn-danger" asp-controller="Courses" asp-action="Delete" asp-route-id="@item.CourseId">
                                                <i class="bi bi-trash-fill"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <p class="text-info">No hay registros para mostrar</p>
                    } *@
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @* <script>
        $(document).ready(function () {
            $('#table_courses').DataTable();
        });
    </script> *@
    <script src="~/js/course.js"></script>
}